apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: card-platform
  labels:
    app: api
spec:
  selector:
    app: api
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
