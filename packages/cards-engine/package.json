{
  "name": "@card-platform/cards-engine",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "echo \"No linter configured yet\" && exit 0",
    "generate-svgs": "ts-node --esm scripts/generate-svgs.ts"
  },
  "dependencies": {
    "@card-platform/shared-types": "*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/__tests__/**/*.test.ts"],
    "transform": {
      "^.+\\.ts$": ["ts-jest", {
        "tsconfig": {
          "module": "CommonJS",
          "moduleResolution": "Node"
        }
      }]
    }
  }
}
